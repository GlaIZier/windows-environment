---
- name: Ensure Chocolatey itself is installed
  win_chocolatey:
    name: chocolatey
	state: present

# Installs not in Program files directory: https://chocolatey.org/docs/default-chocolatey-install-reasoning
- name: Ensure applications are installed
  win_chocolatey:
    name: "{{ item }}"
    state: present
  with_items: "{{ chocolatey_apps }}"

	

